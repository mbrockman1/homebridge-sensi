{
  "pluginAlias": "SensiPlatform",
  "pluginType": "platform",
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "type": "string",
        "title": "Name",
        "default": "Sensi"
      },
      "refreshToken": {
        "type": "string",
        "title": "Sensi Refresh Token",
        "description": "Paste the refresh token obtained from Emerson Sensi. See Readme.md for instructions"
      }
    },
    "required": [
      "name",
      "refreshToken"
    ]
  }
}